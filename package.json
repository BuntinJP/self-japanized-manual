{
  "name": "self-japanized-manual",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "preview:worker": "bun run build && wrangler pages dev",
    "deploy:worker": "bun run build && wrangler pages deploy"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.31.0",
    "astro": "^5.1.5",
    "sharp": "^0.32.5"
  },
  "devDependencies": {
    "wrangler": "^3.101.0"
  }
}
